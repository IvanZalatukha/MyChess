This chess application is written in java. 
The program can be used in the mode of two players or one player (clases GameGUI and GameGUIEngine).
To play against the computer, you need to install the Stockfish engine(in the class Stockfish you should point the way).